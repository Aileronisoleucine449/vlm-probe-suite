Helpers to fetch the subsets we use. No data is shipped in this repo.

remember `chmod +x scripts/*.sh` after clone -- git mode bit isn't restored everywhere.

Winoground is gated -- you need to accept the dataset terms on HF first.
Then `huggingface-cli login` and re-run.
